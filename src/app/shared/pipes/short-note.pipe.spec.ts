import { ShortNotePipe } from './short-note.pipe';

describe('ShortNotePipe', () => {
  it('create an instance', () => {
    const pipe = new ShortNotePipe();
    expect(pipe).toBeTruthy();
  });
});
