import { CalculoHorasPipe } from './calculo-horas.pipe';

describe('CalculoHorasPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculoHorasPipe();
    expect(pipe).toBeTruthy();
  });
});
